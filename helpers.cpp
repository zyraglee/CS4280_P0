#include "traversals.h"
#include "buildtree.h"

using namespace std;
void caseToLower(string& input){
        for(int i = 0; i < input.length(); i++){
                input[i] = tolower(input[i]);
        }
}

